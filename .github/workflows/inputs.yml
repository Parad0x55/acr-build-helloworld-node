name: Mixed inputs

on:
  workflow_dispatch:
  
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: |
          echo ${GITHUB_REF}
          echo "GIT_TAG=`echo $(git describe --tags --abbrev=0)`"
